export default function ListFilter() {
  return (
    <select name="" className="w-full border p-3 rounded-lg">
      <option value="default">Sort by default</option>
      <option value="default">Sort by default</option>
      <option value="default">Sort by default</option>
      <option value="default">Sort by default</option>
    </select>
  );
}
